# V0.2: live-прогон OpenRouter (Windows)

Дата прогона: 2026-09-24, ~14:08 UTC. Оператор: агент по поручению владельца проекта.
Итог: **live gate V0.2 закрыт для конфигурации `openrouter:~typesafe/jev-latest`**.
Typesafe (прямой Jev API) остаётся неподтверждённым.

## Конфигурация

| Параметр | Значение |
|---|---|
| ReflexMesh | `7c034121cda264b7f28c24b97978ccaded91aaf9`, `reflexmesh_dirty=false` |
| JevRouter | `f944acb6530621bced023352e2358a63218bf4d9`, `npm ci --ignore-scripts` + `npm run build` |
| Среда | Windows 11 10.0.26200 (a-moryakov-nb), Python 3.13.15 (venv), Node v24.15.0 |
| Провайдер `/health` | `openrouter:~typesafe/jev-latest` |
| Фактическая модель | `typesafe/jev-1.13-20260917`, OpenRouter provider `TypeSafe` (из `raw_jev` и ledger OpenRouter) |
| Policy | JevRouter по умолчанию, `policy_hash sha256:2d89a115cfd2515a9e9dd7e1f976b2ff0efdf441b4d5066e8b419a52dbc9f205`; порог не менялся (`policy_sha256` в metadata = null: отдельный файл policy не передавался) |
| Сервер | `node dist/cli.js serve --provider openrouter --port 8797` в `C:\work\JevRouter` |

Отклонения от [гайда](../guides/V0.2-live-run-windows.md):

- Порт **8797** вместо 8787: 8787 на машине занят сторонним процессом (MaestroBridge relay).
  Сборщик запущен с `--jev-url http://127.0.0.1:8797`.
- OpenRouter отвечает HTTP 403 на любые запросы с IP машины (RU). Поэтому исходящий
  трафик **только процесса JevRouter** к `openrouter.ai:443` шёл через CONNECT-туннель
  (`NODE_USE_ENV_PROXY=1`, `HTTPS_PROXY` → `ssh -R` → allowlist-прокси на Linux-хосте, UZ).
  Путь ReflexMesh → JevRouter остался прямым loopback без proxy; содержимое TLS
  прокси не видит и не меняет.
- Первый прогон (`v02-live-evidence-openrouter-1`) без туннеля: `jev_auth_error`
  («OpenRouter rejected the API key») из-за геоблока. Он сохранён как диагностика и
  считается **fail**, а не частью закрытия gate.

## Результат сборщика

`collect_v02_live.py`, каталог `C:\private\v02-live-evidence-openrouter-2`, exit 0,
`mechanical_checks_passed: true`, `live_gate_closed: false` (как задумано: закрытие — ручное).

| Case | Вход allowed_routes | Exit | Итог | decision_id / OpenRouter generation |
|---|---|---|---|---|
| multi | CUA, LLM, PERCEPTION | 0 | selected `LLM`, confidence 1, `jev_choice=LLM`, fallback нет | `dec_ebaf9052-d701-4868-bb02-098a299a91ff` / `gen-dec-1790258911-iNgVO8UImjeMvAGsE62w` |
| filtered | LLM, PERCEPTION | 0 | selected `LLM`, CUA не отправлялся | `dec_59add5e9-29f1-4902-bb42-7a3d806206a7` / `gen-dec-1790258912-Vnrt2covlgUoNApwhSBi` |
| empty | — | 3 | abstained / `no_eligible_route`, `request_sent=false`, новых receipts нет | — |

Все механические проверки `true`: совпадение набора кандидатов с receipt,
`decision.selected` = `route`, provider, SHA-256 ответа = SHA-256 receipt
(`8c5cbe62…bd2b` для multi, `4bcf94be…b9e7` для filtered), `is_stub=false`,
`execution_performed=false`.

## Независимое подтверждение вызова модели

`GET https://openrouter.ai/api/v1/generation?id=…` с тем же ключом вернул обе генерации
(workspace ключа, `api_type: decisions`, `origin: github.com/jevrouter/jevrouter`,
модель `typesafe/jev-1.13-20260917`, provider TypeSafe, status 200):

| Generation | created_at (UTC) | tokens in/out | cost, $ |
|---|---|---|---|
| gen-dec-1790258911-… (multi) | 2026-09-24T14:08:31.441Z | 495 / 44 | 0.00002079 |
| gen-dec-1790258912-… (filtered) | 2026-09-24T14:08:32.72Z | 448 / 36 | 0.000018816 |

Токены и стоимость совпадают с `raw_jev.usage` в receipts; время совпадает с запуском
сборщика; число генераций (2) равно числу сетевых запросов сборщика.

## Пять пунктов live-пакета

| # | Пункт | Отметка |
|---|---|---|
| 1 | Commit обоих репозиториев, среда, провайдер/модель, policy без ключей | **pass** |
| 2 | Входы/выходы/exit codes связаны с upstream receipts; selected, разрешённый route, `execution_performed=false` | **pass** |
| 3 | Успешный запрос с 3 кандидатами; ID точные; `decision.selected` = route; `jev_choice` = selected, fallback нет | **pass** |
| 4 | CUA отсутствует в отправленном наборе filtered и не выбран; пустой набор — локальный abstained без сетевого вызова | **pass** |
| 5 | Реальный вызов провайдера подтверждён ledger OpenRouter (выше) | **pass** |

## Сопутствующие наблюдения

- `python -m unittest discover -s tests` на Windows/Python 3.13: 22 теста,
  **1 fail** — `test_v02.JevTests.test_timeout_and_unreachable`: для закрытого
  loopback-порта ожидается `transport_error`, получено `transport_timeout`
  (на Windows отказ в соединении приходит после повторов SYN, дольше тестового
  timeout 0.1 с). Это недостаток теста для Windows, а не live-пути; на Linux (3.12) тесты проходят.
  Исправлено в 599b58d; см. [расширенные сценарии](V0.2-live-extended-openrouter.md).
- `npm` в PowerShell заблокирован ExecutionPolicy; использовался `npm.cmd`.

## Хранение свидетельств

Оригинальный пакет не изменялся: `C:\private\v02-live-evidence-openrouter-2` на
a-moryakov-nb (копия у оператора). Ключей в пакете нет (проверено поиском `sk-or`).
Receipts содержат только тестовую фразу задачи и ответ модели (выбор маршрута).
Пакет в репозиторий не добавляется. SHA-256 файлов:

```
41f119094b8b2471d2ca19bbb8f334e17c08eb3018b3548b58321b6d45d1eb13  report.json
38c4588013d5e705b89aa811bae47301a4782fb0e058cf356f04614dfde2be50  metadata.json
9ce774a2dc07c3d9ea5bf5ca7f7163cf0f06a396d802ae736ae28f03eaf33333  multi.input.json
53a1cdc36c0f5f69b895a9b24d06dc7f86476dbaafb3899b25e19ec23aed6299  multi.stdout.json
6aaeed9dff6ab6998d1feb601fc04ef2a36e3e2d052130b6f88552646f706117  filtered.input.json
f4fb43004f5550a7ee1e0298dd93437a87e6cfb9ddfc6e0d9d3cdef53e6d33cd  filtered.stdout.json
9e7d7f60875cfcd51536750543207397a1c04d1f30916b72d5de088bcbd3df6c  empty.input.json
ae9b7d4d17ba8aaf4069e90f396d9e19d271319e8bd6fd0c036e4396aeffa48d  empty.stdout.json
8c5cbe6251923c976ed486d3e052e727ee720b676e75578c931df6664c70bd2b  decisions/dec_ebaf9052-d701-4868-bb02-098a299a91ff.json
4bcf94be80b035b2d65b93d9f5293ea44d5635e9ca73a836ffae67347e25b9e7  decisions/dec_59add5e9-29f1-4902-bb42-7a3d806206a7.json
```

Использованный OpenRouter-ключ (лимит $0.5) подлежит ротации владельцем.
