# V0.5 deterministic acceptance (U) — 2026-09-25

**Result: 36 passed / 36 planned; 0 failed; 0 pending within U.** The complete V0.5 gate remains open because browser (B) and live Jev (J) attempts have not run.

The [U manifest](../../experiments/v05/u-manifest.json) expands every U subcase in [specification section 9](../specs/V0.5.md#9-fixed-acceptance-gate-r7) into an expected outcome and an exact unittest identifier. SHA-256: `0b05989b37cc274a521dd785e32585a05373c71535d4c98a5c933a1fd8a05509`. Several subcases share one test that makes multiple assertions; there are 36 subcase rows and 33 distinct test identifiers. Every row was executed once.

The [machine-readable results](V0.5-U-results.json) retain all planned rows, individual durations, status, manifest digest, and the evaluated Git commit `1887114c51214068c97119e1f302bb247afc9344`. An [earlier exploratory attempt](V0.5-U-initial-results.json) retained 20 passed and 16 pending rows; it was not substituted for the final attempt.

Environment: Linux x86_64, Python 3.12.14; SystemOneHarness commit `ab8e8f08b4a6268c0633a474423d556599ee06a4`; Browser Use is absent from the U test path. The providers were deterministic fakes or the harness script; no live model was queried. Exact invocation from the repository root (with the sibling optional dependencies installed as described in the [local guide](../guides/V0.5-local-run.md)):

```bash
PYTHONPATH=src:../.deps:../SystemOneHarness python experiments/v05/run_u.py \
  --output docs/research/V0.5-U-results.json
```

The separate full regression command `PYTHONPATH=src:../.deps:../SystemOneHarness python -m unittest discover -s tests -q` passed **85/85** tests before the commit, against the same source tree. The U manifest was then run against the committed tree.

U verifies local state transitions and fixture logic with fault injection: deadline and cancellation ordering, hung calls and cleanup, repeated uncertain mutations, result mapping, exact input validation, run isolation, and redacted artifacts. A fake verifier's passing assessment is an input to these transition tests; it does not establish a real browser effect. B/J must establish real selector-map behavior, authoritative fixture consequences, and live provider behavior at their required repetition counts. Additional runtime-constraint and evidence-schema work remains documented in [implementation progress](V0.5-implementation-progress.md).
