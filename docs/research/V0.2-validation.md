# Проверка V0.2

Дата: 2026-09-23. Linux, Python 3.12.14, Node.js 24.19.0.
Проверено рабочее дерево, включённое в коммит с этим отчётом.

- `python -m unittest discover -s tests -v`: 21 тест прошёл.
  Сохранены 10 тестов V0.1; добавлены 11 проверок V0.2 с подслучаями.
- JevRouter checkout `f944acb6530621bced023352e2358a63218bf4d9`:
  `npm ci --ignore-scripts` и `npm run build` успешно.
- `python scripts/check_jevrouter_http.py --upstream-dir <checkout>`:
  настоящий JevRouter HTTP server + его demo provider;
  selected, low-confidence abstained, demo_not_allowed — успешно.
- `git diff --check`: успешно.

Live Jev: НЕ ПРОВЕРЕН. Переменные TYPESAFE_API_KEY, JEV_API_KEY,
OPENROUTER_API_KEY в среде отсутствовали. Demo доказывает совместимость
HTTP/контракта, но не доступность, качество или стоимость модели.
Windows и Python 3.11 отдельно не проверялись.

При первом тестовом цикле обнаружено принятие порта 0 как значения по умолчанию;
исправлено, негативный тест сохранён. В среде отдельные tool-вызовы не дали
надёжного доступа к долгоживущему серверу; воспроизводимый скрипт запускает сервер
и клиент в одном процессе-оркестраторе и завершает сервер после проверки.

## Первый live-прогон

Инструкции установки и запуска находятся в README. Сначала проверяется одна
ограниченная подзадача, затем исключение маршрута через allowed_routes,
затем пустой набор (без модели). Результат выбранного маршрута не является
успехом исходной задачи. Сравнение качества начинается только в V0.3.
