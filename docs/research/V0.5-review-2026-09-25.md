# V0.5 — независимая проверка (`fe088e6`), 2026-09-25

Проверка состояния V0.5 после коммитов `49b5ef3…fe088e6`. Это ревью и воспроизведение,
а не закрытие gate: B и J остаются pending. Спецификация: [V0.5](../specs/V0.5.md).

## Воспроизведено

- `unittest discover -s tests`: **85/85** на Python 3.13.7 с SOH `ab8e8f08b4a6268c0633a474423d556599ee06a4`
  (базовые отчёты — Python 3.12; на 3.13 тоже проходит).
- U-манифест (sha256 `0b05989b37cc274a521dd785e32585a05373c71535d4c98a5c933a1fd8a05509`) на
  `fe088e6bb29d12a16e3a4f79d9fa796537538b8d`: **36/36 passed**, те же ID подслучаев.
- **Первый B smoke в настоящем Chromium — pass** (не входит в зачёт B: 1 проход, без манифеста).
  Сценарий «форма», `reflexmesh run --routing-provider stub --action-provider script`:
  `completed / verified / pass`; действия navigate, type_text name, type_text email,
  submit_form — все `applied`; на сервере ровно один `POST /form` с правильными значениями слотов;
  шагов 5, dispatches 4, 5.2 с. Сквозной путь CLI → supervisor → SOH → Browser Use → verifier работает.

## 1. Провенанс отчётов

[`V0.5-implementation-progress.md`](V0.5-implementation-progress.md),
[`V0.5-U-acceptance.md`](V0.5-U-acceptance.md) и [`V0.5-U-results.json`](V0.5-U-results.json)
ссылаются на коммит `1887114`, которого нет в опубликованной истории (вероятно, rebase/squash).
Предлагается перепрогнать U на опубликованном коммите и сохранить это как новую попытку.
Исходный файл оставить с пометкой, что коммит недоступен. Воспроизведение выше на `fe088e6`
(36/36) можно приложить.

## 2. Совместимость адаптера с Browser Use 0.13.10 на настоящем Chromium

Пробы на стенде V0.5 (`experiments/v05/site/server.py`), Chromium 153.0.8010.12 headless.

| Допущение `FixtureBrowser` | Факт |
|---|---|
| `data-reflex-id` доступен в `node.attributes` | ✅ |
| `backend_node_id` присутствует | ✅; совпадает с индексом кандидата |
| Перечитывание selector map без изменений страницы | ✅ те же ID |
| Reload страницы | ✅ новые `backend_node_id` → старый кандидат `stale` |
| Замена узла (`replaceWith(cloneNode)`) с тем же `data-reflex-id` | ✅ новый `backend_node_id` → требование §5 о замене узла выполнено |
| Идентичность страницы/документа | ❌ `target_id` — это вкладка: он не меняется при навигации `/form` → `/settings`; в кортеже `_map()` идентичности документа нет |
| Идентичность фрейма | ❌ `frame_id` всегда `None` в 0.13.10 |
| enabled, форма назначения (form action) | ❌ не входят в кортеж `_map()` и не проверяются в `admit`, хотя §5 их требует |

Предложения: добавить в идентичность `loaderId` документа через CDP (`Page.getFrameTree`);
добавить в кортеж `disabled` и `form.action`/`formaction`; iframe и узлы без атрибутов явно
считать `adapter_contract_unsupported`; покрыть это тестами на настоящем браузере, а не только
на моках.

## 3. Ограничения выполнения (§8)

Реализовано только `runtime.budget`. Нет строк для соблюдения разрешений, неизменности слотов,
единоличного владения сессией, порядка dispatch и отсутствия повторов неясных действий.
По спецификации при неполных данных они должны быть `unknown`, но сейчас их просто нет в `verification`.

## 4. C13 — отзыв политики

Ревизии политики в коде нет: `policy_revision` не встречается ни в `runner.py`, ни в адаптере.
Отзыв после выбора кандидата не на чём проверить.

## 5. Остальные пробелы runtime

Совпадают с [отчётом о ходе работ](V0.5-implementation-progress.md), подтверждены по коду:

- сверка неясных действий по состоянию сервера сделана не для всех случаев;
- падение worker при удержании gate не показано тестом (ожидание с ограничением есть);
- сроки завершения дерева процессов не измерены, хотя `os.setsid()`/`os.killpg()` есть.

## 6. Готовая среда для B/J

- a-moryakov-nb, отдельный WSL-дистрибутив `ReflexMeshV04` (Ubuntu 24.04).
- SOH `ab8e8f0` в `/opt/rm/SystemOneHarness/.venv` вместе с Browser Use 0.13.10.
- Chromium 153.0.8010.12: `/opt/rm/ms-playwright/chromium-1243/chrome-linux64/chrome`.
- ReflexMesh `fe088e6`: `/opt/rm/ReflexMesh`.
- **Под root Chrome не стартует** (sandbox): запускать от пользователя `rm`.
- **OpenRouter с IP машины отвечает 403.** Для J HTTPS провайдера идёт по цепочке: мост в WSL
  → `python.exe` на Windows → `ssh -R` → CONNECT-прокси с allowlist на хосте оператора
  (`HTTPS_PROXY=http://127.0.0.1:18080`, `NO_PROXY=127.0.0.1,localhost`).
- Ориентиры для J по [V0.4](V0.4-spike.md): около 2 с на решение Jev через туннель, около $0.003
  за 12 прогонов.

## Порядок до B/J

1. п.2 — адаптер: идентичность документа, enabled, форма назначения, iframe, тесты на настоящем браузере.
2. п.3–5 — runtime: строки ограничений, ревизия политики для C13, сверка неясных действий,
   U-тесты на падение с удержанием gate и сроки очистки.
3. Заморозить манифест B/J: команды, версии, провайдеры, ожидаемые поля.
4. B — по 3 прохода на свежих фикстурах.
5. J — по 3 прохода с JevRouter (macro) и Jev (micro).
